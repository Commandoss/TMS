document.addEventListener("keyup", outputKeyboardEvent);
document.addEventListener("keydown", downKeyboardEvent);


function outputKeyboardEvent(event) {
    // if(event.)
    console.log(event)
}

function downKeyboardEvent(event) {
    if(event.key === "Shift") {

    }
    console.log(event)
}